#include <stdio.h>
int main()
{
	int sum=0;
	int count=0;
	int number=0;
	printf("请输入一个数，输入-1表示输入结束");
	scanf("%d",&number);
	while(number!=-1){
		count++;
		sum=sum+number;
		printf("请输入一个数");
		scanf("%d",&number);
	}
	printf("%f,%d\n",1.0*sum/count,count);
	return 0;
}
